version: '3'

services:
  redis:
    image: redis:6.2.6
    restart: always
    container_name: redis_local
    ports:
      - "6379:6379"
    command: redis-server --save 20 1 --loglevel warning
    volumes:
      - cache:/data

volumes:
  cache:
    driver: local
